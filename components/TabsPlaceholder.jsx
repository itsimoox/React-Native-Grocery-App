import { View } from 'react-native';
import { bgColor } from '../utils/constants';

const TabsPlaceholder = () => <View style={{width: '100%', height: 75, backgroundColor: bgColor}} />

export default TabsPlaceholder;